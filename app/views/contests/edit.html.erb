<% content_for :title, "Editing #{@contest.title}" %>

<p>
<%= render 'form' %>
</p>
<% if false # COMMENTED %>
<p>
<b> Problems: </b>
<% @problems.each do |problem| %>
  <%= link_to problem.title, problem_path(problem) %>
  (<%= link_to "remove", :controller => "problem_contest", :action => "remove", :method => "post", :problem_id => problem, :contest_id => @contest %>)
<% end %>
</p>
<% end %>
<%= link_to 'Show', @contest %>
