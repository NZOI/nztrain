language: ruby
rvm:
  - "1.9.3"
  - "2.0.0"

# uncomment this line if your project needs to run something other than `rake`:
#before_script: # for Selenium RSpec tests
#  - "export DISPLAY=:99.0"
#  - "sh -e /etc/init.d/xvfb start"

before_script:
  - RAILS_ENV=development DATABASE_USERNAME=postgres DATABASE= TEST_DATABASE=nztraintest SCHEDULE_BACKUPS=0 ISOLATE_ROOT=/ ISOLATE_CGROUPS=false bash script/install/config.bash --defaults
  - AUTOCONFIRM=true script/install.bash --skip-update
  - bundle exec rake db:test:load
script: bundle exec rspec

